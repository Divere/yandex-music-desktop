{
  "name": "music.desktop-client",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build:mac": "electron-packager . Y.Music --platform=darwin --overwrite --out=build --ignore=build --icon=og-image.icns",
    "build:win": "electron-packager . Y.Music --platform=win32 --overwrite --out=build --ignore=build --icon=og-image-512.ico",
    "build": "npm run build:mac && npm run build:win"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^15.2.0",
    "electron-packager": "^15.4.0"
  }
}
